# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Data validation / models ---
pydantic>=2.8,<3.0

# --- Database & migrations (PostgreSQL example) ---
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
alembic>=1.13,<2.0

# --- Async HTTP client (for WhatsApp / external APIs) ---
httpx>=0.27,<1.0

# --- Env vars management ---
python-dotenv>=1.0,<2.0

# --- Background jobs (optional at first, but good to plan) ---
redis>=5.0,<6.0
celery>=5.4,<6.0

# --- AI client (example: OpenAI – you can swap later) ---
openai>=1.0,<2.0

# --- Dev tools (optional but recommended) ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
black>=24.0,<25.0
isort>=5.13,<6.0
mypy>=1.10,<2.0
